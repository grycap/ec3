---
- hosts: localhost
  connection: local
  roles:
  - role: grycap.nomad
    name: server
    bind_address: "{{ IM_NODE_PRIVATE_IP }}"
    server_enabled: false
    client_enabled: true
    use_consul: true
    consul_address: "{{nomad_server_ip}}:8500"
    create_nomad_service: true
